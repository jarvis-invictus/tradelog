// M4.6 — FCM permission request + token save
// Call AFTER onboarding complete, NOT before
// TODO: Build in M4.6 milestone session
export function useNotifications() {
  return { requestPermission: async () => {} }
}
