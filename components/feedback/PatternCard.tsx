// M7.4 — Pattern card: raw numbers only (trades, win rate %, net P&L ₹)
// Only renders when pattern has 5+ trades
// TODO: Build in M7.4 milestone session
export default function PatternCard() {
  return null
}
