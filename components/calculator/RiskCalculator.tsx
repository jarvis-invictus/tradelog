// M5.2 — Risk calculator: lot size, P&L ₹, RR ratio
// Inputs: entry, SL, balance, risk%. Recalculates on every keystroke.
// TODO: Build in M5.2 milestone session
export default function RiskCalculator() {
  return null
}
