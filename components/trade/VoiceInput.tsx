// M4.2 — Hold-to-speak voice input → POST /api/whisper → editable textarea
// Never stores audio, only transcript
// TODO: Build in M4.2 milestone session
export default function VoiceInput() {
  return null
}
