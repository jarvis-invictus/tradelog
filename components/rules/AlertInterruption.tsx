// M8.3 — Level 2 hard stop overlay (cannot dismiss by tapping outside)
// Daily loss limit: ONE button only ("Stop for today") — no escape hatch
// TODO: Build in M8.3 milestone session
export default function AlertInterruption() {
  return null
}
