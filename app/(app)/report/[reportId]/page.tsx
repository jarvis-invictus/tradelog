export const dynamic = 'force-dynamic'
// M10.5 — Past report by ID
// TODO: Build in M10.5 milestone session
export default function PastReportPage({ params }: { params: { reportId: string } }) {
  return null
}
