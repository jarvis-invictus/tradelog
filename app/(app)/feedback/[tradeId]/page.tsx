export const dynamic = 'force-dynamic'
// M7.3 — FeedbackScreen
// TODO: Build in M7.3 milestone session
export default function FeedbackPage({ params }: { params: { tradeId: string } }) {
  return null
}
