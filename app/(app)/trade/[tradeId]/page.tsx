export const dynamic = 'force-dynamic'
// M4.1 — Trade detail view
// TODO: Build in M4.1 milestone session
export default function TradeDetailPage({ params }: { params: { tradeId: string } }) {
  return null
}
